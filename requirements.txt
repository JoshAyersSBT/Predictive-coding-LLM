accelerate>=0.30
datasets>=2.19
pyarrow>=15.0
pyyaml>=6.0
safetensors>=0.4
tokenizers>=0.19
torch>=2.2
tqdm>=4.66
transformers>=4.41

# ROCm users: install requirements-rocm.txt instead of this file so pip uses
# the ROCm PyTorch wheel index.
